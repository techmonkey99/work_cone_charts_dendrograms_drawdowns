# Packages needed by hedge_fund_analytics.py.
#   pip install -r requirements.txt
pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
openpyxl>=3.1
# Optional: interactive HTML line charts in module 3. Without it, module 3
# falls back to static charts automatically.
plotly>=5.18
